import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Escorts in Surat | Premium Companion Services",
  description: "Experience luxury and elegance with our premium companion services in Surat. Professional, sophisticated, and discreet companionship for all occasions.",
  keywords: "escorts, surat, companion services, luxury, premium, professional",
  openGraph: {
    title: "Escorts in Surat | Premium Companion Services",
    description: "Experience luxury and elegance with our premium companion services in Surat.",
    type: "website",
    locale: "en_US",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen flex flex-col`}
      >
        <Header />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
